```cpp
// 创建GN902对象
int Create_GN902(int & id){
        return 0;
}

// 初始化GN902对象
// @param id 算法id
// @param phsDiffThreshold 位相差阈值数组
// @param satelliteCountThreshold 卫星数量阈值数组
// @param cutCountThreshold 切刀次数阈值数组
// @return 正确运行返回0，错误id返回1
int Init_GN902(int id, double* phsDiffThreshold, double* satelliteCountThreshold, 
            double* cutCountThreshold){
        return 0;
}


// 设置GN902对象数据
// @param id 算法id
// @param data 数据数组
// @param cutIdx 通道2对应天线
// @param endFlag 结束标志（是否为最后一帧数据，0为不是，1为是）
// @return 正确运行返回0，错误id返回1
int SetData_GN902(int id, const GNSSData* data, int cutIdx, int endFlag){
        return 0;
}


// 获取GN902对象结果
// @param id 算法id
// @param result 结果结构体
// @return 正确运行返回0，错误id返回1
int GetResult_GN902(int id, double& angle){
        return 0;
}

// 释放GN902对象
// @param id 算法id
// @return 正确运行返回0，错误id返回1
int Release_GN902(int id){
        return 0;
}
```