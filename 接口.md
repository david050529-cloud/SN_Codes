```cpp
// 创建GN902对象
// @param id 算法id
// @return 正确运行返回0，错误id返回1
int Create_GN902(int & id){
        return 0;
}

// 设置欺骗检测阈值
// @param id 算法id
// @param phsDiffThreshold 位相差阈值(度)
// @param satelliteCountThreshold 卫星数阈值(相位差聚簇的最小卫星数)
// @param cutCountThreshold 连续确认刀数(>0 时生效)
// @param sysEnum 卫星系统编码
// @param typeEnum 频点编码
// @return 正确运行返回0，错误id返回1
int SetThresholdDetection_GN902(int id, double phsDiffThreshold, double satelliteCountThreshold,
            double cutCountThreshold, int sysEnum, int typeEnum){
        return 0;
}

// 设置GN902对象数据
// @param id 算法id
// @param data 数据数组
// @param cutIdx_1 通道1天线索引
// @param cutIdx_2 通道2天线索引
// @return 正确运行返回0，错误id返回1
// 天线对 {8,9} 或 {9,8} 为固定基线采集模式：不参与循环切刀、不进入测向，
// 仅逐星采集两端口载波相位差并写入日志文件(./gn902_phasediff.log)。
int SetData_GN902(int id, const GNSSData* data, int cutIdx_1, int cutIdx_2){
        return 0;
}

// 获取GN902对象结果
// @param id 算法id
// @param result 结果结构体
// @return 正确运行返回0，错误id返回1
int GetResult_GN902(int id, SpoofingResult& result){
        return 0;
}

// 释放GN902对象
// @param id 算法id
// @return 正确运行返回0，错误id返回1
int Release_GN902(int id){
        return 0;
}
```

---

## 固定基线相位差采集（天线对 {8,9}/{9,8}）

`SetData_GN902` 的 `cutIdx_1`/`cutIdx_2` 分别为通道1/通道2 天线索引。当传入天线对为
`{8,9}` 或 `{9,8}` 时进入固定基线采集模式：

- **不参与循环切刀**：该帧不进入「校正刀 {1,1} + 六刀测向 {1,2}..{1,7}」的循环流程；
- **不进入测向**：不触发欺骗检测与相关干涉仪测向（Detect/Doa）；
- **只采集相位差**：逐星计算两端口载波相位差（取小数部分，单位周），写入
  `./gn902_phasediff.log`（追加模式）。

每帧日志格式：

```text
[固定基线采集] 天线对=(8,9) 双端卫星数=N
  PRN=xx, Sys=x, Type=x, Snr1=xx.x, Snr2=xx.x, PhaseDiff=0.xxxxxx周(xx.x度)
```

> 该模式**按帧判定**：仅当本帧天线对为 `{8,9}`/`{9,8}` 时采集；
> 一旦出现其它天线对（如 `{1,1}..{1,7}`）即恢复循环切刀流程。

### main902 测试模拟

`main902` 用可选 `fixedPair` 配置项模拟固定基线采集（不依赖 debug 切刀时刻表，
把每个公共 GPS 秒都作为该固定天线对喂入）：

```json
{
    "method": 90,
    "method type": 0,
    "datDir": "E:/gn/jiance_flow/0902/data",
    "logFile": "E:/gn/jiance_flow/0902/gn902_result.log",
    "fixedPair": [8, 9]
}
```

- 不加 `fixedPair`：保持原有「循环切刀检测 + 测向」，`debugFile` 必需；
- 加 `fixedPair: [8,9]` 或 `[9,8]`：固定基线采集，`debugFile` 可省略。