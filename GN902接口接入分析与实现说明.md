# GN902 接口接入：分析与实现说明（2026-09-06）

> 依据：`接口.md`（GN902 主机侧接口契约）与 `SpoofingDoaDll` 现状。
> 结论：**现有通用接口需要（且已）通过新增一层 GN902 适配接口来对接**；原通用接口保持向后兼容。

---

## 1. 分析：为什么现有接口不能直接接收 GN902 数据

| 维度 | 现有通用接口（interface.h / interface.cpp） | GN902 契约（接口.md） |
|---|---|---|
| 数据送法 | `setSpoofingDoa(id, GNSSData*, dataLen)` 一次传一整轮数组，内部按固定 `m_cutSequence × m_OneCut_Frams` 分刀 | `SetData_GN902(id, GNSSData*, cutIdx, endFlag)` **逐帧流式**，每帧带通道2天线号与整轮结束标志 |
| 初始化 | 逐次 `setThresholdDetectionDoa(...)`（每次一个频点） | `Init_GN902(id, phs[], satCount[], cutCount[])` 一次给三组数组 |
| 结果 | 返回丰富 `SpoofingResult`（多频点 × 多星 + DOA/质量） | `GetResult_GN902(id, double& angle)` 只要单角 |
| 切刀语义 | 引擎假定刀序/每刀帧数固定 | 每帧 `cutIdx` 实时给出，帧数每轮自适应 |

→ 故在 `SpoofingDoaDll` 内新增 `interface_GN902.h/.cpp`，把 GN902 流式输入缓冲成整轮后喂给 `SpoofingDoa` 引擎（循环切刀检测，与当前 Python 流程一致）。

---

## 2. 实现内容

| 文件 | 说明 |
|---|---|
| `SpoofingDoaDll/interface_GN902.h` | 5 个导出函数声明 + 约定注释 |
| `SpoofingDoaDll/interface_GN902.cpp` | 整轮缓冲、阈值装载、单角输出实现 |
| `SpoofingDoaDll/SpoofingDoa.h/.cpp` | 新增公共方法 `configCyclicRuntime(cyclic, oneCutFrams, smooth, omniR)`：让设备 wrapper 在 `Init` 之后覆盖循环切刀运行参数并按 GN902 半径(0.1865)重建理论模板 |
| `SpoofingDoaDll/CMakeLists.txt` | 注册 `interface_GN902.cpp` |

### 生命周期用法

```c
int id; double angle;
Create_GN902(id);                        // 建对象(内部即 GN902 循环检测配置)
// 可选：阈值数组长度>=12，顺序见 §3.1；传 NULL 表示用默认
double sat[] = {...}, phs[] = {...}, cut[] = {2};
Init_GN902(id, phs, sat, cut);           // 配置阈值 + 连续确认 p
// 每 1s 一帧逐帧送入。
// cutIdx=1(校正刀 code=0 / 天线对1-1)的帧会自动储存为校正数据(每轮有则刷新)；
// cutIdx=2..7 为六刀测向刀。endFlag=1 表示六刀测向轮结束，触发一次“校正+六刀”检测+测向。
SetData_GN902(id, &gnssCal, 1, 0);        // (可选)校正刀数据，存为校正
SetData_GN902(id, &gnssFrame, cutIdx, 0); // 测向轮中间帧(cutIdx=2..7)
...
SetData_GN902(id, &gnssLast, cutIdxLast, /*endFlag*/1); // 六刀测向轮最后一帧 -> 触发
GetResult_GN902(id, angle);              // angle=欺骗方位角(0..359)，无结果=-1
Release_GN902(id);
```

---

## 3. 约定（`接口.md` 未文档化处，按 Python 流程对齐，请主控侧核对）

### 3.1 cutIdx = 通道2 对应天线号（1..7）

> **测向只用六刀**：第一刀 code=0（天线对 1-1）为**校正刀，不参与测向**；凡是 cutIdx=1 传入的数据先**存为校正数据**（每轮出现则刷新，供后续测向轮校正）。

| cutIdx | 天线对 | 对应 Python code | 用途 |
|---|---|---|---|
| 1 | {1,1} | 0 | **校正刀**（存为校正数据，不参与测向） |
| 2 | {1,2} | 9 | 测向刀 |
| 3 | {1,3} | 57 | 测向刀 |
| 4 | {1,4} | 17 | 测向刀 |
| 5 | {1,5} | 25 | 测向刀 |
| 6 | {1,6} | 33 | 测向刀 |
| 7 | {1,7} | 1 | 测向刀 |

引擎切刀行 j = cutIdx−1：row0 = 校正(cutIdx1)，row1..6 = 六刀测向。**测向轮需覆盖六刀（cutIdx=2..7，每刀至少 1 帧）**；校正数据已储存或本轮含有均可，缺校正或六刀缺帧时本轮丢弃（不破坏跨轮连续确认/跟踪状态）。

### 3.2 Init_GN902 阈值数组（顺序固定，长度 ≥ 12）

| 下标 | 频点 | Sys,Type |
|---|---|---|
| 0 | GPS L5C | 0,2 |
| 1 | GLONASS G1 | 1,0 |
| 2 | GLONASS G2 | 1,1 |
| 3 | Galileo E1C | 3,2 |
| 4 | Galileo E5a | 3,12 |
| 5 | Galileo E5b | 3,17 |
| 6 | BDS B1I | 4,0 |
| 7 | BDS B3I | 4,2 |
| 8 | BDS B1C | 4,8 |
| 9 | BDS B2I | 4,17 |
| 10 | BDS B2b | 4,19 |
| 11 | BDS B1X | 4,34 |

- `satelliteCountThreshold[i]`：卫星颗数阈值（**严格大于**触发，如 2 → 聚类 ≥3 颗报警）；
- `phsDiffThreshold[i]`：相位差阈值（度）；
- 其余频点回退 DLL 默认（卫星数 2、相位差 5°）。

### 3.3 cutCountThreshold

- `cutCountThreshold[0]` = 连续确认刀数 p（对应 Python `ALARM_CONSECUTIVE_P`，建议 2）。多余元素预留。

### 3.4 帧数自适应（每轮）

- 校正数据：cutIdx=1 的帧被**储存**（`calFrames`），每轮出现则刷新为最新校正。
- 六刀测向轮（cutIdx=2..7）：
  - 六刀每刀帧数一致为 C、且校正帧 ≥ C 时 → 取校正帧末尾 C 帧与六刀全帧喂入引擎，启用 Python 式「稳定性过滤 + 圆形均值」（对应 dat 8 帧/刀平滑路径）；
  - 帧数不一致或校正帧不足 → 每行只取**末尾 1 帧**（最稳定观测，对应 Python dat 路径按刀末稳定秒喂帧）。
- 跨轮连续报警计数 `m_ConsecutiveAlarm` 与测向跟踪 `m_Tracking` 在引擎内**持续累积**，不因轮次结束而清零（与 Python 循环切刀流程一致）。

### 3.5 结果

`GetResult_GN902` 把整轮各频点测向角度做圆均值作为单角（0..359）；无任何有效角度时置 `angle=-1`。

---

## 4. 关联

- 检测/校正/测向数值口径与 Python 的对齐见 `Python_Cpp_欺骗检测测向流程一致性对比与修改记录_2026-09-06.md`。
- 引擎默认配置 `SpoofingDoaDll/DoaBSpoofingConfig.txt`（cyclicDetectionFlag=1、detectionRecoddsNum=2、r=0.1865 等）与新接口一致；GN902 wrapper 不依赖该文件存在，`Create_GN902` 会显式覆盖半径/切刀/循环参数。
