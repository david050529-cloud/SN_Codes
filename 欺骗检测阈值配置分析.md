# 欺骗检测阈值配置分析

## 配置日志

```
Sys=4,Type=17,coutThreshold=5,phsThreshold=-1.0
Sys=4,Type=0,coutThreshold=5,phsThreshold=-1.0
Sys=4,Type=2,coutThreshold=5,phsThreshold=-1.0
Sys=4,Type=19,coutThreshold=5,phsThreshold=-1.0
Sys=1,Type=0,coutThreshold=3,phsThreshold=5.0
Sys=-1,Type=-1,coutThreshold=5,phsThreshold=5.0
Sys=0,Type=2,coutThreshold=4,phsThreshold=5.0
Sys=1,Type=0,coutThreshold=3,phsThreshold=5.0
Sys=1,Type=1,coutThreshold=4,phsThreshold=5.0
Sys=3,Type=2,coutThreshold=4,phsThreshold=3.6
Sys=3,Type=12,coutThreshold=4,phsThreshold=3.6
Sys=3,Type=17,coutThreshold=4,phsThreshold=3.6
Sys=4,Type=34,coutThreshold=4,phsThreshold=3.6
```

---

## 逐行解读

日志来自 `setThresholdDetectionDoa` 函数（`SpoofingDoaDll/SpoofingDoa.cpp:1057`），**按调用顺序输出**，后面的调用会覆盖前面同频点的设置。

```
① Sys=4,Type=17, coutThreshold=5, phsThreshold=-1.0   → BDS B2I
② Sys=4,Type=0,  coutThreshold=5, phsThreshold=-1.0   → BDS B1I
③ Sys=4,Type=2,  coutThreshold=5, phsThreshold=-1.0   → BDS B3I
④ Sys=4,Type=19, coutThreshold=5, phsThreshold=-1.0   → BDS B2b
⑤ Sys=1,Type=0,  coutThreshold=3, phsThreshold=5.0    → GLONASS G1

⑥ Sys=-1,Type=-1, coutThreshold=5, phsThreshold=5.0   ⚠️ 通配符：重置全部频点！
                                                       所有频点 → threshold=5, phs=5度

⑦ Sys=0,Type=2,  coutThreshold=4, phsThreshold=5.0    → GPS L5     (单独覆盖⑥)
⑧ Sys=1,Type=0,  coutThreshold=3, phsThreshold=5.0    → GLONASS G1 (单独覆盖⑥，与⑤相同)
⑨ Sys=1,Type=1,  coutThreshold=4, phsThreshold=5.0    → GLONASS G2 (单独覆盖⑥)
⑩ Sys=3,Type=2,  coutThreshold=4, phsThreshold=3.6    → Galileo E1C
⑪ Sys=3,Type=12, coutThreshold=4, phsThreshold=3.6    → Galileo E5a
⑫ Sys=3,Type=17, coutThreshold=4, phsThreshold=3.6    → Galileo E5b
⑬ Sys=4,Type=34, coutThreshold=4, phsThreshold=3.6    → BDS B1X (军码)
```

---

## 最终生效配置汇总

| 频点编码 | 系统 | 信号 | 卫星颗数阈值 | 相位差阈值(度) | 相位差阈值(周) | 虚警风险 |
|----------|------|------|:----------:|:-------------:|:-------------:|:--------:|
| `100` | GLONASS | G1 | **3** | 5.0° | 0.0139 | 🔴 **高** |
| `002` | GPS | L5 | 4 | 5.0° | 0.0139 | 🟡 中 |
| `101` | GLONASS | G2 | 4 | 5.0° | 0.0139 | 🟡 中 |
| `302` | Galileo | E1C | 4 | **3.6°** | 0.0100 | 🟡 中 |
| `312` | Galileo | E5a | 4 | **3.6°** | 0.0100 | 🟡 中 |
| `317` | Galileo | E5b | 4 | **3.6°** | 0.0100 | 🟡 中 |
| `434` | BDS | B1X (军码) | 4 | **3.6°** | 0.0100 | 🟡 中 |
| `400` | BDS | B1I | 5 | 5.0° | 0.0139 | 🟢 低 |
| `402` | BDS | B3I | 5 | 5.0° | 0.0139 | 🟢 低 |
| `417` | BDS | B2I | 5 | 5.0° | 0.0139 | 🟢 低 |
| `419` | BDS | B2b | 5 | 5.0° | 0.0139 | 🟢 低 |
| 其余全部 | — | — | **5** | 5.0° | 0.0139 | 🟢 低 |

---

## 关键发现

### 1. `Sys=-1` 通配符放在中间，是一个脆弱的配置顺序

第⑥行将前5行的设置全部抹掉，然后⑦~⑬再逐个覆盖回来。如果⑧没有重复设 GLONASS G1，它就会被第⑥行的 threshold=5 覆盖，丢失 threshold=3 的配置。这种"先通配、后特例"的写法虽然功能正确，但第⑧行本质上是对第⑤行的冗余补救。

### 2. GLONASS G1 仍然是最敏感的频点

threshold=3（只需3颗卫星相位差相近即告警）＋ phs=5度，在开阔天空下容易产生虚警。

### 3. Galileo 三个频点 + BDS B1X 使用了更严格的相位差阈值

phs=3.6°（0.01周），比其他频点的5°更严。这意味着同样的卫星，在 Galileo 频点上更不容易被聚类到一起。这可能是针对这些频点特性的调优——频率更高、天线基线相对更长，相位差区分度更好，所以可以用更小的窗口。

### 4. BDS 民用频点(B1I/B2I/B3I/B2b)使用了最高的卫星颗数阈值

threshold=5，需要至少5颗被欺骗卫星才告警。如果干扰源只模拟了3-4颗 BDS 卫星，则系统不会告警——**这是以检测灵敏度换虚警抑制**。

### 5. 配置覆盖关系示意

```
初始默认:        全部 → thr=4,  phs=5°
构造函数覆盖:    BDS×4 → thr=5, phs不变
                GLONASS G1 → thr=3, phs=5°
                  ↓
配置文件第⑥行:   全部 → thr=5, phs=5°     ← 重置！
                  ↓
配置文件⑦~⑬:    单独覆盖10个频点
```

---

## 建议

| 问题 | 建议 |
|------|------|
| `Sys=-1` 行位置不当 | 将通配符行移到配置文件最开头，后面全是特例覆盖，逻辑更清晰 |
| ⑧行与⑤行重复 | 如果配置文件在构造函数之后加载，⑤行是冗余的（会被⑥覆盖），建议清理 |
| GLONASS G1 阈值仍为3 | 如果仍然有虚警，考虑提高到4 |
| GPS/SBAS/QZSS 未单独配置 | 它们被⑥行设为 threshold=5，可能存在检测灵敏度不足的问题 |
