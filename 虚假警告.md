# 欺骗信号检测逻辑完整分析

## 一、整体架构

```
输入 GNSSData ──→ 相位差计算 ──→ 按频点分类 ──→ 三级检测 ──→ 滑动窗口确认 ──→ 输出结果
                  (getSatellite    (getSatellite   (calAlarm    (setDetection   (Spoofing
                   DataPhaseDiffA)  DataByType)     By...)       Records)        Result)
```

系统的核心假设是：**欺骗信号由同一个干扰源发射，因此多颗被欺骗卫星在同一频点上会表现出"同源性"特征**——相位差相近、到达角相近、载噪比相近。

---

## 二、前置处理：相位差计算 (`PreparationData.cpp`)

1. 遍历两个通道（两个天线端口）的各颗卫星
2. 匹配同系统、同频点、同PRN的卫星对
3. 计算载波相位差：`phase_diff = (port1.Phase - port2.Phase)`，取小数部分归一化到 [0, 1) 周
4. 输出 `SatelliteDataPhaseDiffA` 结构，包含 PRN、SNR1、SNR2、相位差

```cpp
// PreparationData.cpp:101-109
double phs_tp = data.i_PortOne[i].i_Phase - data.i_PortTwo[j].i_Phase;
double diff = phs_tp - (long long int)phs_tp; // 取小数部分
if (diff < 0) diff = diff + 1;
tp.i_phase_diff = diff;
```

---

## 三、三级检测方法 (`Alarm.cpp`)

### 方法1：相位差聚类法 — `calAlarmByPhaseDiff` (第30行)

这是**主检测方法**，在每个频点内独立运行：

**算法流程：**

```
For 每颗卫星 i（作为参考星）:
    count = 0
    For 每颗卫星 j:
        if Sj的SNR1或SNR2接近0: 跳过
        diff = |PhaseDiff_i - PhaseDiff_j|
        if diff < 相位差阈值(默认5度, 即 5/360 ≈ 0.0139周):
            count++  // 认为j与i来自同一方向
    
    记录最大聚类组和刚好达到(阈值-1)的组

// 最终判决：
if 最大聚类数 >= 卫星颗数阈值:
    alarm = 1  // 直接判定为欺骗
elif 最大聚类数 == (阈值 - 1):
    双重确认：检查该组内卫星载噪比是否也相近(差值≤1dB)
    只有载噪比也聚类时，才 alarm = 1
else:
    alarm = 0
```

**关键阈值（构造函数 `SpoofingDoa.cpp:25-29`）：**

| 频点 | 卫星颗数阈值 | 相位差阈值 |
|---|---|---|
| BDS B1I (sys=4,type=0) | 5 | 5度 |
| BDS B2I (sys=4,type=17) | 5 | 5度 |
| BDS B3I (sys=4,type=2) | 5 | 5度 |
| BDS B2b (sys=4,type=19) | 5 | 5度 |
| GLONASS G1 (sys=1,type=0) | **3** | 5度 |
| 其他频点 | 4（默认） | 5度 |

---

### 方法2：载噪比聚类法 — `calAlarmBySnr` (第244行)

> ⚠️ **默认未启用**（所有频点 `m_Detection_snrThrehold = -1`），仅在相位差法失败时作为备选。

**算法流程：**

```
For 每颗卫星 i:
    对通道1：统计 SNR1 差值 ≤1dB 的卫星数量
    对通道2：统计 SNR2 差值 ≤1dB 的卫星数量
    取两个通道聚类数的更大值
    如果最大聚类数 >= 卫星颗数阈值: alarm = 1
```

---

### 方法3：到达角聚类法 — `calAlarmByAngle` (第140行)

> 仅在**完整测向模式**（`m_Detection_Tag == 0`）的最终结果汇总阶段使用。

**算法流程：**

```
For ang = 0 ~ 359度:
    以 ang 为中心，统计角度差 < m_Angle_Threshold(默认3度) 的卫星
    注意360度循环: diff = min(|ang - satAngle|, 360 - |ang - satAngle|)
    
找到聚集卫星数最多的角度组
if 最大聚集数 >= 卫星颗数阈值: alarm = 1
最终角度 = 聚集组中最频繁出现的具体角度值
```

---

## 四、滑动窗口确认机制 — `setDetectionRecords` (第318行)

**这是防止虚警的关键环节**：

```
维护一个长度为 N 的队列 (默认 N=1)
每帧 push_back(当前alarm), pop_front(最旧alarm)
只有当队列中所有N帧的 alarm 都是1时，才最终输出 alarm=1
```

**重要**：默认 `m_Detection_Recodds_Num = 1`，即没有滑动窗口保护，**单帧检测结果直接生效**。

---

## 五、完整判决流程

### 仅检测模式 (dataLen=1)：

```
GNSSData → getSatelliteDataPhaseDiffA → getSatelliteDataByType → getAlarm()
                                                                  │
                                                      ┌───────────┴───────────┐
                                                      │ calAlarmByPhaseDiff   │
                                                      │   (相位差聚类)         │
                                                      │         ↓             │
                                                      │ alarm==0? → calAlarmBySnr │
                                                      │         ↓             │
                                                      │ setDetectionRecords    │
                                                      │         ↓             │
                                                      │ alarm==1: 保存告警卫星  │
                                                      └───────────────────────┘
```

### 完整测向模式 (dataLen>2)：

```
多帧 GNSSData → 逐帧相位差计算 → 校正数据补偿 → (可选)欺骗检测筛选
                                                   ↓
                                         getSatelliteDataPhaseDiffB
                                                   ↓
                                         干涉仪/幅相法 DOA 测向
                                                   ↓
                               calAlarmByAngle（角度聚类确认）
                                                   ↓
                                         最终 SpoofingResult
```

---

## 六、虚假告警根因分析

根据代码分析，虚假告警可能来自以下几个方面：

### 🔴 原因1：相位差阈值过于宽松（最可能）

`m_Phasediff_Threshold = 5度` → 转换为 0.0139 周。对于实际场景：
- 天线阵列通常较小（半径 ~0.2m），在 GPS/BDS 频率下（λ ≈ 0.19m），相位差的理论范围约 ±0.5 周
- 5度 / 360度 = 1.4% 的相对范围，在**真实多星场景中，如果有几颗卫星偶然位于相近方向**，它们的相位差可能恰好落入同一窗口

**量化分析**：7阵元圆阵，基线长度约0.2m，两星方向差 Δθ=5° 时，相位差变化量约为 `2π·r/λ·(sin(θ+Δθ)-sin(θ))` ≈ 几度量级。**自然条件下两颗卫星很难相位差正好差不到5度**。但如果天线阵列较小或频率较低，**相位差区分度就会降低，导致不同方向的卫星也被聚类到一起**。

### 🔴 原因2：卫星颗数阈值过低

| 频点 | 阈值 | 风险 |
|---|---|---|
| BDS (sys=4) | 5颗 | 中 |
| GLONASS G1 | **3颗** | **高** ⚠️ |
| 其他 | 4颗 | 中 |

GLONASS G1 仅需 **3颗卫星相位差相近** 即判定欺骗。在开阔天空下3颗GLONASS卫星同方向出现的概率虽然不高，但不是不可能。

### 🟡 原因3：滑动窗口默认长度为1

```cpp
int m_Detection_Recodds_Num = 1;  // SpoofingDoa.h:447
```

`probability < 1` 才重置（而不是 `probability < 0.5` 之类的多数投票）——必须连续N帧**全部**为1。但 N=1 意味着没有抗干扰能力，**单帧偶然波动就会触发告警**。

### 🟡 原因4：相位差计算本身存在歧义

```cpp
// PreparationData.cpp:104
double diff = phs_tp - (long long int)phs_tp;
```

载波相位差的 **整数周模糊度** 被直接丢弃。在以下情况下会出错：
- 相位差超过1周时，小数部分无法区分"真的相位差接近"还是"相差了整数周恰好小数部分接近"
- **跳半周现象**（`getDetection180` 和 `setPermutationOptimiz180` 函数的存在本身就说明项目中确实存在此问题）

### 🟡 原因5：载噪比过滤逻辑可能漏过异常数据

```cpp
// Alarm.cpp:48
if (dataA[j].i_Snr1 < 1e-3 || dataA[j].i_Snr2 < 1e-3)
    continue;
```

只过滤了载噪比接近0的卫星。但低载噪比（如 20 dB-Hz）的卫星相位噪声很大，其相位差随机波动，**可能偶然落入其他卫星的聚类窗口内，提高聚类计数**。

### 🟡 原因6：双重确认机制的边界情况

```cpp
// Alarm.cpp:82-117
if (maxCount == (m_Detection_Threshold[typeInt] - 1))
{
    // 双重确认: 检查组内卫星的载噪比是否也一致
    if (count2 == maxCount) alarm = 1;
}
```

阈值-1的特殊处理：当最大聚类刚好差一颗才到阈值时，系统会寻找"第二组同样大小的聚类"，再在其中检查载噪比一致性。**但这是在"刚好差一颗"的前提下才触发**。如果 maxCount 直接等于阈值，则**跳过载噪比验证**直接告警。

### 🟡 原因7：单通道数据混入

```cpp
// PreparationData.cpp:115-125
if (flg_E)
{
    tp.i_Snr2 = 0;        // 第二通道无信号
    tp.i_phase_diff = -1;  // 无效相位差！
}
```

只在通道1有信号而通道2没有的卫星，其 `phase_diff = -1`，在相位差聚类中 -1 周等同于 0 周（归一化后），如果多个这样的单通道卫星存在，它们的"相位差"都是 -1，**全部会被聚类到一起**，人为增加聚类计数。

---

## 七、建议的改进方向

| 优先级 | 改进项 | 方法 |
|---|---|---|
| 🔴 高 | 增加滑动窗口长度 | `setDetectionRecordNum(3~5)`，需要连续3-5帧检测到才告警 |
| 🔴 高 | 提高相位差阈值精度 | 根据实际天线阵列参数计算更合理的阈值，当前5度可能偏大 |
| 🔴 高 | 排除单通道数据 | `calAlarmByPhaseDiff` 中过滤 `phase_diff < 0` 的无效数据 |
| 🟡 中 | 加入载噪比下限过滤 | 设置合理的最低载噪比（如 >30 dB-Hz），排除低质量数据 |
| 🟡 中 | 时钟/频率一致的卫星才聚类 | 真实同方向卫星应具有相似的多普勒，可增加多普勒一致性检查 |
| 🟢 低 | 双重确认扩展到所有情况 | 即使 maxCount>=阈值，也强制要求载噪比一致性验证 |
| 🟢 低 | 增加空间分布合理性检查 | 欺骗信号应集中在一个方向，真实信号应均匀分布在全天 |
